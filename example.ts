// example.ts

/**
 * A function that doubles the input number.
 * @param input - The number to be doubled.
 * @returns The doubled number.
 */
function doubleNumber(input: number): number {
    return input * 2;
  }
  
  export default doubleNumber;
  